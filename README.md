# Customer Segmentation — RFM Analysis

RFM (Recency, Frequency, Monetary) analysis pipeline for customer segmentation and targeted marketing strategy.

## Overview
Segments customers into actionable groups (Champions, Loyal, At Risk, Lost, etc.) using RFM scoring.

## Usage
```bash
pip install -r requirements.txt
python main.py --input data/transactions.csv
```
